const CoffeeShop = require('../models/coffeeshop');

const CoffeeShopsController = {

	updateStock(req, res) {
		// Update product stock after an order has been processed

	},

};

module.exports = CoffeeShopsController;